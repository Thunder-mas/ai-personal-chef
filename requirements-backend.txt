# 后端（FastAPI + React 同源）部署依赖 —— 用于 Hugging Face Spaces 单容器。
# 仅 api/server.py 这条链路所需：FastAPI 服务 + LangGraph Agent + RAG + 多模态。
# 不含 streamlit / notebook / langgraph-cli / dashscope（那些是别的入口用的）。
fastapi>=0.128.8
uvicorn>=0.39.0
openai>=1.6.1,<2.0.0
python-dotenv>=1.0.1
numpy
fastembed>=0.7.4
langchain-core>=0.3.86
langchain-openai>=0.3.35
langchain-tavily>=0.2.11
langgraph>=0.6.11
langgraph-checkpoint-sqlite>=2.0.11
# Redis 缓存（可选）：未配 REDIS_URL 时自动回落进程内缓存，不装也能跑。
# redis-py 是纯 Python、体积小；装上以便在云端设置 REDIS_URL 后即开即用。
redis>=5.0.0
# 注：chromadb（Chroma 向量库后端）刻意不进生产依赖——它较重，且线上默认走零依赖的
# numpy 后端（RAG_BACKEND=numpy）。需要 chroma 后端时本地 `uv pip install chromadb` 即可。
